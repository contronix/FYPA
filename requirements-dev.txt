# Development-only dependencies (test + lint tooling).
# Install alongside the runtime deps:
#   .venv\Scripts\python.exe -m pip install -r requirements.txt -r requirements-dev.txt
pytest==9.0.3
ruff==0.15.13
