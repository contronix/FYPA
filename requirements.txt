# FYPA runtime dependencies.
# Pinned to the versions used during development (Python 3.11). Looser
# constraints likely work but haven't been tested.
#
# altium_monkey is NOT listed here — it's a git submodule installed via
#   pip install -e altium_monkey
# See the README for the install steps.

# --- Qt + GUI ---
PySide6==6.11.1

# --- Custom OpenGL viewer ---
PyOpenGL==3.1.10
PyOpenGL_accelerate==3.1.10

# --- FEM solver + mesher (pdnsolver/) ---
numpy==2.2.3
scipy==1.17.1
shapely==2.1.2
triangle==20250106

# Fast direct linear solver (MKL PARDISO). Optional but strongly
# recommended — several times faster than scipy's SuperLU on large boards.
# pdnsolver.solver falls back to SuperLU automatically if it is absent.
# Pulls in Intel MKL (~180 MB).
pypardiso==0.4.7

# --- Colormaps + CPU-side hover-probe interpolator ---
matplotlib==3.10.9
